### --- Tmux Configuration ---
# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Set tmux prefix to Ctrl+B and Ctrl+A
set -g prefix C-b
set -g prefix2 C-a

# Enable mouse mode!
set -g mouse on
